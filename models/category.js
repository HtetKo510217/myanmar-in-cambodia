class Category {
    constructor(id, title, color, iconName) {
        this.id = id;
        this.title = title;
        this.color = color;
        this.iconName = iconName;
    }
}

export default Category;
